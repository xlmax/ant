# TODO

Незакрытые и будущие доработки Ant. Каждый пункт — отдельная идея/проблема, которую можно брать в работу.

## Известные проблемы / шум

- [ ] **Уведомление об обновлении при ожидании ввода.** Сейчас проверка блокирующая (таймаут до 20 с) и нотис появляется до промпта. Хорошо бы показывать его мгновенно, не задерживая ввод (без сложной фоновой логики).
- [ ] **Проверить унифицированный многострочный ввод в терминалах.** VT/PTY-backend с bracketed paste и переносимым `Ctrl+J` нужно вручную проверить в Orca, Herdr, Windows Terminal, Android, Linux и macOS; нативная Windows Console сохраняет Win32 backend.

## Модели / vision

- [ ] **Определение поддержки изображений — «костыль».** Сейчас `vision` выводится из имени модели (`/vision/i`). DeepSeek `/models` не отдаёт capabilities, поэтому автодетект условный. Если появится vision-модель без «vision» в id — сломается. Рассмотреть явный список/настройку `model.visionModels` с fallback на эвристику.

## Надёжность / тесты / CI

- [ ] **Windows-интеграционные тесты.** Сейчас юнит-тесты покрывают только чистые функции (`hasPendingKeyDown`, `consoleWidth` логику), но не сам вызов `PeekConsoleInputW`/`GetConsoleScreenBufferInfo` и не реальный ввод. Нужен прогон на Windows.
- [ ] **`console-size.ts`** — добавить юнит-тест на fallback-ветки (не-Windows путь, недоступный хэндл).

## Оптимизация расходов

- [ ] **Расход токенов в длинных сессиях.** Vision-модель + `thinking: high` — самый дорогой расклад. Подумать: дефолтный `thinking` ниже для рутины, подсказки по `/compact`, автоподсказка смены модели.
- [ ] **Пересчёт контекста** — следить, чтобы оценка `/context` совпадала с фактической отправкой (в т.ч. с учётом изображений).

## Прочее

- [ ] **CHANGELOG** — вести журнал изменений по релизам (сейчас только README и git-теги).
- [ ] **Обработка изменения размера окна** — полосы-разделители тянутся на Windows (через `GetConsoleScreenBufferInfo`), но стоит перепроверить поведение при live-resize на всех ОС.
